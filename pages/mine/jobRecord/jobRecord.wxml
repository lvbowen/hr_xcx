
 <scroll-view class="scroll-wrap" scroll-y  bindscrolltolower="loadMore">
    <view class="record-item" wx:for="{{recordList}}" wx:key="positionApplyId">
        <view class="positionName">
            <text class="text font-34">{{item.positionName}}</text>
            <text class="status" class="{{'status_fail':item.currentStatus=='已淘汰'}}">{{item.currentStatus}}</text>
          </view>
          <view class="companyName">
            <image class="company_icon" src="../../../images/company3.png"></image>
            <text class="company_text font-gray-26">{{item.companyName}}</text>
          </view>
          <view class="applyTime font-gray-26">申请时间：{{item.createTime}}</view>
    </view>
 </scroll-view>
